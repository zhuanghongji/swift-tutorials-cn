# AxisMarkBuilder

<ToBePolishedAfterTranslation />

<OriginalSource
  title="AxisMarkBuilder"
  url="https://developer.apple.com/documentation/charts/axismarkbuilder"
  summary="构造轴标记并覆盖默认标记的结果生成器"
/>

```swift
@available(iOS 16.0, macOS 13.0, tvOS 16.0, watchOS 9.0, *)
@resultBuilder public struct AxisMarkBuilder {
    // ...
}
```

## 类型方法

### buildBlock()

```swift
public static func buildBlock() -> some AxisMark
```

### buildEither(first:)

```swift
// 为多语句闭包中的 "if-else" 语句提供支持，为 "then" 分支生成条件内容
public static func buildEither<T1, T2>(first: T1) -> BuilderConditional<T1, T2> where T1 : AxisMark, T2 : AxisMark
```

### buildEither(second:)

```swift
// 为多语句闭包中的“if-else”语句提供支持，为“else”分支生成条件内容
public static func buildEither<T1, T2>(second: T2) -> BuilderConditional<T1, T2> where T1 : AxisMark, T2 : AxisMark
```

### buildExpression(\_:)

```swift
public static func buildExpression<Content>(_ content: Content) -> Content where Content : AxisMark
```

### buildIf(\_:)

```swift
// 为多语句闭包中的“if”语句提供支持，生成仅当条件计算为 true 时才可见的可选轴内容
public static func buildIf<T>(_ content: T?) -> T? where T : AxisMark
```

### buildLimitedAvailability(\_:)

```swift
// 为多语句闭包中带有 #available() 子句的“if”语句提供支持，为“then”分支（即有条件可用的分支）生成条件内容
public static func buildLimitedAvailability<Content>(_ content: Content) -> some AxisMark where Content : AxisMark
```

### buildPartialBlock(accumulated:next:)

```swift
public static func buildPartialBlock(accumulated: some AxisMark, next: some AxisMark) -> some AxisMark
```

### buildPartialBlock(first:)

```swift
public static func buildPartialBlock<T>(first content: T) -> T where T : AxisMark
```
