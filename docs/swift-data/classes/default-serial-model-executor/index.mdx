# DefaultSerialModelExecutor

<OriginalSource
  title="DefaultSerialModelExecutor"
  url="https://developer.apple.com/documentation/swiftdata/defaultserialmodelexecutor"
  summary="使用独立的（isolate）模型上下文安全地执行存储相关任务（storage-related tasks）的对象"
/>

```swift
@available(swift 5.9)
@available(macOS 14, iOS 17, tvOS 17, watchOS 10, *)
public class DefaultSerialModelExecutor : @unchecked Sendable, SerialModelExecutor {
    // ...
}
```

## 初始化器

### init(modelContext:)

```swift
public init(modelContext: ModelContext)
```

## 实例属性

### modelContext

```swift
final public let modelContext: ModelContext
```
