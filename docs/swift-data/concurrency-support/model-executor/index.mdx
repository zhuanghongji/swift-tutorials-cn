# ModelExecutor

<OriginalSource
  title="ModelExecutor"
  url="https://developer.apple.com/documentation/swiftdata/modelexecutor"
  summary="用于使用独立的（isolated）模型上下文执行存储相关任务（storage-related tasks）的接口"
/>

```swift
@available(swift 5.9)
@available(macOS 14, iOS 17, tvOS 17, watchOS 10, *)
public protocol ModelExecutor : Executor {
    // ...
}
```

## 实例属性

### modelContext

```swift
var modelContext: ModelContext { get }
```
