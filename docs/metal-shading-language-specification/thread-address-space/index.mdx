# 线程地址空间

<OrigninalPDF title="4.3 thread Address Space" />

线程地址空间是指每个线程的内存地址空间。在此地址空间中分配的变量对其它线程不可见。在图形或内核函数内部声明的变量在线程地址空间中分配。

```
[[kernel]] void my_kernel(...) { // 在每线程地址空间中分配的浮点型 float x; }
```

``` // 指向每个线程地址空间中变量 x 的指针 thread float \* p = &x; ````
