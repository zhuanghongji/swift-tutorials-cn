# 关系函数

<ToBePolishedAfterTranslation />

<OrigninalPDF title="6.4 Relational Functions" />

表 6.3 中的关系函数在头文件 `<metal_relational>` 中定义。`T` 是标量或向量浮点类型之一，包括 `bfloat` 类型。`Ti` 是标量或向量整数或布尔类型之一。`Tb` 仅指标量或向量布尔类型。

<TableView
  num="6.3"
  title="Relational functions in the Metal standard library"
  headers={["Built-in Relational Functions", "Description"]}
  datasList={[
    ["bool all(Tb x)", "Returns true only if all components of x are true."],
    ["bool any(Tb x)", "Returns true only if any component of x are true."],
    ["Tb isfinite(T x)", "Test for finite value."],
    ["Tb isinf(T x)", "Test for infinity value (positive or negative)."],
    ["Tb isnan(T x)", "Test for a NaN."],
    ["Tb isnormal(T x)", "Test for a normal value."],
    [
      "Tb isordered(T x, T y)",
      "Test if arguments are ordered. isordered() takes arguments x and y and returns the result (x == x) && (y == y).",
    ],
    [
      "Tb isunordered(T x, T y)",
      "Test if arguments are unordered. isunordered() takes arguments x and y and returns true if x or y is NaN; otherwise returns false.",
    ],
    ["Tb not(Tb x)", "Returns the component-wise logical complement of x."],
    [
      "T select(T a, T b, Tb c) \nTi select(Ti a, Ti b, Tb c)",
      "For each component of a vector type, \nresult[i] = c[i] ? b[i] : a[i] \n\nFor a scalar type, \nresult = c ? b : a",
    ],
    [
      "Tb signbit(T x)",
      "Test for sign bit. Returns true if the sign bit is set for the floating-point value in x; otherwise returns false.",
    ],
  ]}
/>
