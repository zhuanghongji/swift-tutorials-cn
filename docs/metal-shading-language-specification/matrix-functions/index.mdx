# 矩阵函数

<OrigninalPDF title="6.6 Matrix Functions" />

表 6. 8 中的函数在头文件 `<metal_matrix>` 中定义。`T` 是 `float` 或 `half`。

<TableView
  num="6.8"
  title="Matrix functions in the Metal standard library"
  headers={["Built-in Matrix Functions", "Description"]}
  datasList={[
    [
      "float determinant(floatnxn) \nhalf determinant(halfnxn)",
      "Compute the determinant of the matrix. The matrix needs to be a square matrix.",
    ],
    [
      "floatmxn transpose(floatnxm) \nhalfmxn transpose(halfnxm)",
      "Transpose a matrix.",
    ],
  ]}
/>

例子：

```c
float4x4 mA;
float det = determinant(mA);
```
