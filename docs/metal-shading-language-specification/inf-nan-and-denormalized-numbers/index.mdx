# INF、NaN 和非规范化数

<ToBePolishedAfterTranslation />

<OrigninalPDF title="7.1 INF, NaN, and Denormalized Numbers" />

INF 必须支持单精度、半精度和脑浮点数。

单精度、半精度和脑浮点数必须支持 NaN（禁用快速数学）。如果启用快速数学，则处理 NaN 或 INF（作为输入或输出）的行为是未定义的。不支持发信号 NaN。

作为单精度、半精度或脑浮点算术运算的输入传递或作为输出产生的非规范化单精度、半精度或脑浮点数字可以被刷新为零。
