## 2.1 标量数据类型

Metal 支持表 2 中列出的标量类型。 1. Metal **不**支持 double、long long、unsigned long long 和 long double 数据类型。

## 表 2.1。金属标量数据类型

`类型描述`

``` bool 一种条件数据类型，其值为 true 或 false。值 true 扩展为整数常量 1，值 false 扩展为整数常量 0。````

`char int8_t`

``` 有符号的二进制补码 8 位整数。 ````

`无符号 char uchar uint8_t`

``` 一个无符号 8 位整数。 ````

`短 int16_t`

``` 有符号的二进制补码 16 位整数。 ````

`无符号短 ushort uint16_t`

``` 一个无符号 16 位整数。 ````

`int int32_t`

``` 有符号的二进制补码 32 位整数。 ````

`无符号 int uint uint32_t`

``` 一个无符号 32 位整数。 ````

``` long int64_t 所有操作系统：自 Metal 2.2 起。 ````

``` 有符号的二进制补码 64 位整数。 ````

``` unsigned long uint64_t 所有操作系统：自 Metal 2.2 起。 ````

``` 一个无符号 64 位整数。 ````

``2023 - 06 - 02 |版权所有 © 2023 苹果公司 |版权所有。 ````

`类型描述`

``` 半个 16 位浮点数。 half 数据类型必须符合 IEEE 754 二进制 16 存储格式。 ````

``` bfloat 从 Metal 3.1 开始。 ````

`` 一个 16 位大脑浮点。 bfloat 数据类型是用于机器学习应用程序的 float 的截断版本，使用 8 位（显式存储的 7 位）而不是 24 位尾数）。 ````

``` float 32 位浮点数。 float 数据类型必须符合 IEEE 754 单精度存储格式。 ````

``` size_t sizeof 运算符结果的无符号整数类型。这是一个 64 位无符号整数。 ````

``` ptrdiff_t 有符号整数类型，是两个指针相减的结果。这是一个 64 位有符号整数。 ````

``` void void 类型包含一组空值；它是一种不完整类型，无法完成。 ````

金属支撑：

- f 或 F 后缀指定单精度浮点文字值（例如 0.5f 或 0.5F）。 - h 或 H 后缀指定半精度浮点文字值（例如 0.5h 或 0.5H）。 - bf 或后缀指定大脑精度浮点文字值（例如 0.5bf 或 0.5BF）。 - 无符号整数文字的 u 或 U 后缀。 - 有符号长整数文字的 l 或 L 后缀。

表 2. 2 列出了大多数标量数据类型的大小和对齐方式。

## 表 2.2。标量数据类型的大小和对齐方式

`类型大小（以字节为单位）`

` 对齐方式（以字节为单位）`

`布尔 1 1`

`char int8_t 无符号 char uchar uint8_t`

### 1 1

``2023 - 06 - 02 |版权所有 © 2023 苹果公司 |版权所有。 ````

`类型大小（以字节为单位）`

` 对齐方式（以字节为单位）`

`短 int16_t 无符号短 ushort uint16_t`

### 2 2

`int int32_t unsigned int uint uint32_t`

### 4 4

`long int64_t unsigned long uint64_t`

### 8 8

`尺寸_t 8 8`

`一半 2 2`

``bfloat 2 2````

`浮点数 4 4`
